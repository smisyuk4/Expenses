var keyboard_inline = {     //делает клавиатуру в тексте
  inline_keyboard: [
      [{ 'text': 'за прошлый месяц', 'callback_data': 'month' }],
      [{ 'text': 'за текущий период', 'callback_data': 'period' }]  
    ],
  'resize_keyboard': true  
}
