var keyboard_inline = {     //делает клавиатуру в тексте
  inline_keyboard: [
      [{ 'text': 'Отчёт за месяц', 'callback_data': 'month' }],
      [{ 'text': 'Отчёт за период', 'callback_data': 'period' }]  
    ],
  'resize_keyboard': true  
}
